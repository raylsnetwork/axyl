# Pins the Rust toolchain used to build Rayls Network.
# Kept in sync with the MSRV (`rust-version`) declared in Cargo.toml,
# README.md, and the toolchain installed in CI (.github/workflows/pr.yaml).
[toolchain]
channel = "1.91.0"
components = ["rustfmt", "rust-src", "clippy", "rust-analyzer"]
profile = "minimal"
